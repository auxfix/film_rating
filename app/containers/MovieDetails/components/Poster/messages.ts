import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Poster';

export default defineMessages({
  noPosterImage: {
    id: `${scope}.noImage`,
    defaultMessage: 'no image',
  },
});
