import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.MovieDetails';

export default defineMessages({
  saveRating: {
    id: `${scope}.saveRating`,
    defaultMessage: 'Save rating',
  },
});
